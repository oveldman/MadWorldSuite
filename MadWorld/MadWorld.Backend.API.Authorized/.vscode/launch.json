{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Attach to API Authorized",
        "type": "coreclr",
        "request": "attach",
        "processId": "${command:azureFunctions.pickProcess}"
      }
    ]
  }