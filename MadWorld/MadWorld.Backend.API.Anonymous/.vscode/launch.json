{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Attach to API Anonymous",
        "type": "coreclr",
        "request": "attach",
        "processId": "${command:azureFunctions.pickProcess}"
      }
    ]
  }